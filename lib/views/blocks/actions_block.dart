import 'package:flutter/material.dart';

class ActionsBlock extends StatelessWidget {
  final Map<String, dynamic> configs;
  const ActionsBlock(this.configs, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}