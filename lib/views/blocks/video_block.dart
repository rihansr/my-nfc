import 'package:flutter/material.dart';

class VideoBlock extends StatelessWidget {
  final Map<String, dynamic> configs;
  const VideoBlock(this.configs, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}