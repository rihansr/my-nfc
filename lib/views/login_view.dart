import 'package:flutter/widgets.dart';

class LoginView extends StatelessWidget {
  final Map<String, String>? params;
  const LoginView({this.params, super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}